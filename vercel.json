{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/(.*\\.(js|css|png|jpg|jpeg|svg|ico|json|woff2|woff|map))$", "dest": "/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
